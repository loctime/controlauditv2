# Configuración para paquetes git-hosted
# Aumentar timeout para paquetes git
fetch-timeout=60000
network-timeout=60000

# Configuración para manejar mejor los paquetes git en Vercel
shamefully-hoist=true
strict-peer-dependencies=false
